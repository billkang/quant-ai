schema: spec-driven

context: |
  Tech stack: React + Vite + Ant Design, FastAPI + SQLAlchemy, PostgreSQL + Redis
  Domain: 量化投资辅助系统 (Quant AI)
  Data sources: AkShare (A股), yfinance (港股/美股)
  Deployment: Docker Compose
  AI: DeepSeek API

rules:
  tasks:
    - Focus on MVP features first
    - Use Ant Design components (not raw HTML) for UI
    - Test changes with docker-compose up -d --build
    - FastAPI endpoints use BaseModel classes for POST bodies (query params are legacy)
    - Maintain API response consistency: most endpoints wrap with {code: 0, data, message}

artifacts:
  design:
    - Include architecture decisions and trade-offs
  spec:
    - Use Gherkin-style scenarios (WHEN/THEN)
    - List API endpoints with path and methods
    - Mark response format: wrapped (success_response) or raw (direct return)
  tasks:
    - Break into testable chunks
    - Include verification steps