# Specifications

This change is a pure bug fix with no new or modified capabilities.

- No new requirements are added.
- No existing requirements are modified.
- The AI advice API contract (`POST /api/ai/analyze`) remains unchanged.

The fix addresses an internal parameter-passing error and missing data injection,
both of which are implementation-level issues rather than specification changes.
